node_modules/
web/vendor/
data/*
!data/.gitkeep
# Pages for the billboards, which are content rather than state: somebody wrote them to
# be read, so they travel with the island instead of staying on one machine. See
# lib/billboard.mjs.
!data/boards/
data/boards/*
!data/boards/*.html
!data/boards/*.css
!data/boards/*.js
!data/boards/*.svg
!data/boards/*.png
!data/boards/*.jpg

# per-machine settings; config.example.json is the template
config.json

# never publish credentials or local traces
.env
.env.*
*.log
*.log.1

# build-uitvoer: scripts/build.ps1 zet hier de losstaande Windows-build neer (~250 MB)
dist/
# schermafdrukken van de Godot-harness, opnieuw te maken met scripts/shots.ps1
godot/.captures/
# De screenshot-matrix zelf is ~94 MB aan PNG's en wordt opnieuw gemaakt met
# scripts/shots.ps1. Het contactvel blijft wel: dat legt vast wat er wanneer is
# geschoten, ook als de beelden er niet meer bij zitten.
docs/shots/**/*.png
docs/shots/**/*.jpg
